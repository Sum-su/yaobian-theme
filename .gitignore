# 打包产物（各版本都在 GitHub Releases / 商城上拿）
dist/

# Python
__pycache__/
*.pyc

# 自检/测试的中间件（每次跑 test_mode.py 都会重写）
_test_settings.json
_test_settings.json.yaobian-bak

# mode.py 改设置前留的备份（含别人的密钥块，绝不入库）
*.yaobian-bak

# 发布用的 token（publish.py 读它们；**绝不入库**）
vsce-pat.txt
ovsx-pat.txt
.vsce-pat
.ovsx-pat

# 随手写的探针/临时脚本（下划线开头是约定）。
# **注意别写成 `_*.json`**：`_ref_qingci-{dark,light}.json` 是 gen.py 要用的基准快照，是正经文件。
_*.py
